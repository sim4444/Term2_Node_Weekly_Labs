// Core Node.js modules
const fs = require("fs")

// // Consuming (Using) the readFile function
fs.readFile("file.txt", "utf-8", callback)


function solveRect(length, width){
    rect()
}
function rect(l, w, cb){
    
}

